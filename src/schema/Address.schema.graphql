# TYPES

type Address {
  id: UUID!
  roadNumber: Int
  streetName: String
  city: String
  postalcode: PostalCode
  country: String
}

# INPUTS

input CreateAddress {
  roadNumber: Int!
  streetName: String!
  city: String!
  postalcode: PostalCode
  country: String!
}

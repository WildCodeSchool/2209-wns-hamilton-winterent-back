type User {
  id: ID!
  email: String!
  firstname: String!
  lastname: String
  password: String!
}

type Query {
  users: [User!]!
  user(id: ID!): User
}

type Mutation {
  addUser(
    email: String!, 
    firstname: String!, 
    lastname: String,
    password: String!,
  ): User!
}

entity: Address
items:
  address{1..6}:
    id: '{{datatype.uuid}}'
    roadNumber: '{{address.buildingNumber}}'
    streetName: '{{address.city}}'
    postalCode: 33300
    city: '{{address.cityName}}'
    country: '{{address.country}}'
